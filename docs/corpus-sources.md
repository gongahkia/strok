# Corpus Sources

The web source inventory at `/sources` lists public corpus source URLs with:

- title and publisher
- license and license policy
- last refresh timestamp
- entry count
- failure status

`ok` means the checked-in corpus row has a valid URL, valid `retrieved_at`, and at least one public entry. Other statuses require corpus review before launch.
