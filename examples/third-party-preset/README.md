# Fried Apple Pie Example Preset

Minimal package stub for publishing a third-party preset:

- export `defineFriedApplePiePreset(...)` from `index.ts`
- include a Pi theme with the full token set in a real package
- depend on `fried-apple-pie` and import `fried-apple-pie/sdk`
