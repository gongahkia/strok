name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake pkg-config ffmpeg \
            glslang-tools spirv-cross libvulkan-dev vulkan-tools mesa-vulkan-drivers \
            libavformat-dev libavcodec-dev libavdevice-dev libavutil-dev libswscale-dev libswresample-dev libfreetype-dev zlib1g-dev

      - name: Install macOS dependencies
        if: runner.os == 'macOS'
        run: brew install cmake pkg-config ffmpeg freetype glslang spirv-cross zlib

      - name: Configure
        run: cmake --preset ci

      - name: Build
        run: cmake --build --preset ci

      - name: Test
        run: ctest --test-dir build/ci --output-on-failure

      - name: Smoke test
        run: |
          ./build/ci/contourtty --version
          ./build/ci/contourtty --help >/dev/null
          scripts/verify_terminal_paths.sh ./build/ci/contourtty

  sanitize:
    name: asan-ubsan (ubuntu-latest)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake pkg-config ffmpeg \
            glslang-tools spirv-cross libvulkan-dev vulkan-tools mesa-vulkan-drivers \
            libavformat-dev libavcodec-dev libavdevice-dev libavutil-dev libswscale-dev libswresample-dev libfreetype-dev zlib1g-dev

      - name: Configure
        run: cmake --preset asan-ubsan

      - name: Build
        run: cmake --build --preset asan-ubsan

      - name: Test
        run: ctest --test-dir build/asan-ubsan --output-on-failure

      - name: Decode/render smoke
        run: |
          ffmpeg -hide_banner -loglevel error -f lavfi -i testsrc=size=64x48:rate=5:duration=1 -pix_fmt yuv420p /tmp/contourtty-sanitize.mp4
          ./build/asan-ubsan/contourtty --width 16 --height 8 --dump-frame 1 --dump-png /tmp/contourtty-sanitize.png /tmp/contourtty-sanitize.mp4 >/tmp/contourtty-sanitize.probe

      - name: Shutdown path smoke
        env:
          ASAN_OPTIONS: detect_leaks=1:halt_on_error=1
          UBSAN_OPTIONS: halt_on_error=1
        run: scripts/verify_shutdown_paths.sh ./build/asan-ubsan/contourtty

  valgrind:
    name: valgrind (ubuntu-latest)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake pkg-config ffmpeg valgrind \
            glslang-tools spirv-cross libvulkan-dev vulkan-tools mesa-vulkan-drivers \
            libavformat-dev libavcodec-dev libavdevice-dev libavutil-dev libswscale-dev libfreetype-dev zlib1g-dev

      - name: Configure
        run: cmake --preset ci

      - name: Build
        run: cmake --build --preset ci --target contourtty

      - name: Shutdown path leak checks
        run: |
          cat >/tmp/contourtty-valgrind-wrapper <<'EOF'
          #!/usr/bin/env bash
          exec valgrind --leak-check=full --show-leak-kinds=definite --errors-for-leak-kinds=definite --error-exitcode=99 \
            ./build/ci/contourtty "$@"
          EOF
          chmod +x /tmp/contourtty-valgrind-wrapper
          scripts/verify_shutdown_paths.sh /tmp/contourtty-valgrind-wrapper

  homebrew:
    name: homebrew formula (macos-latest)
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install formula from source
        run: |
          brew untap local/contourtty >/dev/null 2>&1 || true
          brew tap-new local/contourtty
          cp packaging/homebrew/contourtty.rb "$(brew --repo local/contourtty)/Formula/contourtty.rb"
          brew install --HEAD --build-from-source local/contourtty/contourtty

      - name: Test formula
        run: |
          contourtty --version
          brew test local/contourtty/contourtty
