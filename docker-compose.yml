services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: wat
      POSTGRES_PASSWORD: wat
      POSTGRES_USER: wat
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
