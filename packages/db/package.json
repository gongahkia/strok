{
  "name": "@wat/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "seed:dev": "pnpm build && node dist/src/seed-dev.js",
    "test": "vitest run src --exclude 'dist/**'"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "testcontainers": "^10.28.0",
    "vitest": "^4.1.9"
  }
}
