# ER parity fixtures

Fixtures track parser parity against Mermaid ER syntax documented at `https://mermaid.js.org/syntax/entityRelationshipDiagram.html` for version `11.15.0`.

Rejected fixtures are Mermaid-valid syntax that kumeyuri currently fails fast on instead of silently ignoring.
