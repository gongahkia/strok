# Negative Mermaid parser fixtures

These fixtures cover Mermaid inputs called out as diagram-breaking or invalid
in the compatibility backlog. They must fail fast instead of being silently
accepted or partially dropped.
