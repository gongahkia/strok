# Mindmap parity fixtures

These fixtures lock Mermaid mindmap icon fallback text, Markdown labels,
supported shape delimiters, standalone class lines, unclear indentation, and
deep-tree layout behavior.
