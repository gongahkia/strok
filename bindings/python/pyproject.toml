[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "strok"
version = "0.1.0"
description = "Python bindings for the strok C ABI"
requires-python = ">=3.9"
